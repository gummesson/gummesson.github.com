# -- Libraries --

require "rbconfig"

# -- Source --

source "http://rubygems.org"

# -- Gems --

gem "wdm", "~> 0.1.0" if RbConfig::CONFIG["target_os"] =~ /mswin|mingw/i
gem "github-pages"
gem "jekyll-watch"
gem "jekyll-sitemap"
